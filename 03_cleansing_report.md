# 03. 정제 리포트 (SCANIA Component X, 실측)

재현: [scripts/01_pipeline.py](scripts/01_pipeline.py) · 원자료: [outputs/cleansing_stats.json](outputs/cleansing_stats.json)

## 01 — 동기화

- `train_operational_readouts.csv` **1,122,452행**, 23,550대 전원이 `train_tte.csv`/`train_specifications.csv`와 완전히 매칭됨(불일치 0건).
- 차량당 판독 횟수: 최소 **5회**, 중앙값 **43회**, 최대 **303회** — Azure/가상군장비 예시와 달리 **차량마다 판독 빈도가 크게 다르고 공통 시간축이 없다**(논문에 명시된 실측 데이터의 특징). 정렬은 차량별 `time_step` 순서로만 가능.
- 96개 히스토그램 빈은 이번 실행에서 제외하고 8개 단일값 누적카운터만 사용 — [02_domain_mapping.md](02_domain_mapping.md) 참조.

## 02 — 드리프트 제거

- 방법: 차량×채널별 **증분율(rate)** = 카운터 증가량 ÷ time_step 증가량. 각 차량 자신의 과거 이력(마지막 판독 제외)의 평균·표준편차로 z-정규화 → 8채널 |z| 평균을 **이상지수(anomaly_score)**로 사용.
- 1,122,452행 중 **1,098,902행(97.9%)**에서 이상지수 산출 성공(나머지는 차량의 첫 판독이라 증분 계산 불가).
- 누적카운터 특성상 음수 증분은 물리적으로 불가능 — **7건** 발견되어 결측 처리(ECU 카운터 리셋 등으로 논문이 언급한 바로 그 현상으로 추정).

## 03 — 구간 분할

- `train_tte.csv`가 이미 "구성품 장착 이후 경과 time_step"을 제공하므로 별도 분할이 불필요 — **좌측절단 없음**(Azure 5건, 가상군장비 62건과 대조적으로 이 실측 데이터는 관측 시작=장착 시점으로 깨끗하게 기록됨).
- 고장(수리) 이벤트 **2,272건**, 무고장(우측절단) **21,278건**.

## 04 — 결측치 / 이상치

| 카운터 | 결측률 |
|---|---|
| 171_0 | 0.000% |
| 666_0 | 0.004% |
| 427_0 | 0.571% |
| 837_0 | 0.003% |
| 309_0 | 0.003% |
| 835_0 | 0.003% |
| 370_0 | 0.570% |
| 100_0 | 0.570% |

모두 논문이 밝힌 "채널당 1% 미만" 범위 안에 있음을 재확인했다. 이상치 규칙(이상지수>8)으로 **94건(0.0086%)** 플래그.

## 05 — 코드 정합

- 세 파일(readouts/tte/specifications) 간 `vehicle_id` 불일치 **0건**, 중복 (vehicle_id, time_step) 행 **0건** — 실측 데이터임에도 정합성이 매우 높다(SCANIA의 데이터 후처리 파이프라인이 상당히 성숙했음을 시사).

## 06 — 라벨 정의

- 양성 라벨(수리) **2,272건** / 총 노출 **5,660,219.4 time-step**
- 클래스 불균형비: **1,000 time-step당 0.4014건** (양성 비율 0.0401%) — Azure(0.087%)·가상군장비(0.178%)보다도 훨씬 희귀 사건. 실제 부품 고장 데이터의 전형적 극단 불균형.
