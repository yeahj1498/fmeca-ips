# 02. 데이터 → FMECA 매핑 (SCANIA Component X)

## 1. 매핑 가능 필드

| 데이터 필드 | 원본 파일 | 의미 | FMECA 대응 | 비고 |
|---|---|---|---|---|
| `vehicle_id` | train_tte, train_specifications, train_operational_readouts | 차량 익명 식별자 | 분석 대상 식별키 | 23,550대 |
| `Spec_3` (Cat0~Cat3) | train_specifications.csv | 차량 사양 범주(익명화, 엔진형식/휠구성 등 실제 사양 중 하나로 추정) | **FMECA 행(고장모드 그룹) 구분 축** | 4개 범주, 표본 1,871~14,438대로 균형 양호 |
| `length_of_study_time_step` | train_tte.csv | 구성품 장착 이후 경과 time_step(단위 익명화) | 노출(exposure) — O의 분모 | 관측 시작 = 장착 시점으로 이미 정의되어 좌측절단 없음 |
| `in_study_repair` | train_tte.csv | 관측기간 내 Component X 교체/수리 여부(0/1) | **고장모드 라벨 그 자체** | 761건... (실제 값은 outputs 참조), 심하게 불균형 |
| `time_step` (readouts) | train_operational_readouts.csv | 판독 시점(차량마다 비동기) | 정제 01단계 동기화 대상 | 차량별 판독 빈도 상이(논문 명시) |
| `171_0,666_0,427_0,837_0,309_0,835_0,370_0,100_0` | train_operational_readouts.csv | 누적형 카운터 8종(익명화, "trend 표현에 적합"이라고 논문에 명시) | S/D 관측 채널(증분율 기반) | 절대값이 아닌 **증분율**을 채널로 사용(누적값 자체는 심각도를 직접 반영하지 않음) |

## 2. 이번 예시에서 제외한 필드 — 있지만 쓰지 않음

| 필드 | 제외 이유 |
|---|---|
| 히스토그램 변수 6종(`167`,`272`,`291`,`158`,`459`,`397`, 총 96개 빈) | 계산 단순화. 물리적 의미가 완전히 익명화되어 있어(논문도 "어떤 신호인지 밝히지 않음"이라 명시) 빈 단위 해석이 원천적으로 불가능하며, 96개 열을 전부 처리하려면 이번 예시의 범위를 넘는 계산량이 필요함 |
| `Spec_0,1,2,4,5,6,7` | 주축은 Spec_3만 사용. Spec_2는 보너스 사례(§03 FMECA 워크시트)로만 별도 언급 |

## 3. 대응 불가 항목 — 실측 데이터인데도 여전히 없는 것

| 군수정비체계 항목 | 이 데이터에 없는 이유 | 영향 |
|---|---|---|
| 부품 실명/규격(NSN급) | Component X 자체가 영업비밀로 익명화됨 | 어떤 실제 부품을 개선해야 하는지 알 수 없음 — 방법론 실증에는 문제 없으나 실제 조달로 이어지지 않음 |
| 정비 소요공수·비용 실측치 | 논문에 수리 관련 비용 필드 없음(투입인원/작업시간 비공개) | 보급소요의 리드타임·단가는 이번에도 가정치를 사용할 수밖에 없음 |
| 임무·안전 영향(트럭이므로 "임무" 개념 자체가 약함) | 상용차량 데이터라 군사적 심각도 개념이 원천적으로 없음 | S(심각도)는 순수 계측 이상편차 대리지표로만 산출 — 이전 두 예시의 "동시고장"류 2차 축도 이번엔 없음(단일 구성품이라 동시고장 개념 자체가 성립하지 않음) |
| 채널의 물리적 정의 | 8개 카운터·6개 히스토그램 모두 익명화, "무엇을 세는 카운터인지" 비공개 | D(검출도) 산출 시 "왜 이상인지" 설명 불가, "이상치가 있다/없다"만 판단 가능 |
| 운용부대/기후/임무유형 | 상용 트럭 데이터라 군 운용 변수 없음 | O의 층화는 Spec_3(차량사양) 단위까지만 가능 |

## 4. 정리

이 실측 데이터는 이전 두 예시보다 **"현실이 깔끔하지 않다"는 것 자체를 보여주는 좋은 사례**다: 원했던 "3~5개 고장모드"가 아니라 "1개 고장모드 × 4개 사양군"으로 구조가 바뀌었고, 채널 의미는 완전히 비공개라 정성적 해석이 불가능하며, 심각도 축이 하나로 줄었다. 방법론(정제 6단계 → O/S/D 산출 → RCM/LORA/보급/IETM/설계개선)은 동일하게 적용 가능함을 보여주는 것이 이 실행의 핵심 목적이다.
